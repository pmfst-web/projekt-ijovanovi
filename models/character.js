class Character {
  constructor(id, character, image, splash, vision, vision_image, cons) {
    this.id = id;
    this.character = character;
    this.image = image;
    this.splash = splash;
    this.vision = vision;
    this.vision_image = vision_image;
    this.cons = cons;
  }
}

export default Character;
